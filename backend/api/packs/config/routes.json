{
  "routes": [
    {
      "method": "GET",
      "path": "/packs",
      "handler": "packs.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/packs/count",
      "handler": "packs.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/packs/:id",
      "handler": "packs.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/packs",
      "handler": "packs.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/packs/:id",
      "handler": "packs.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/packs/:id",
      "handler": "packs.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
